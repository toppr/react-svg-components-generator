import React from 'react';
import IconBase from '{{iconBaseComponentPath}}';

const {{componentName}} = (props) => (
    <IconBase viewBox="{{viewBox}}" {...props}>
        {{content}}
    </IconBase>
);

export default {{componentName}};
